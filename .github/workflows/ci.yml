# 검사 로직은 kwak-service-infra 에 모여 있다.
# 이 파일은 호출만 한다 — 검사 내용을 바꾸려면 infra 쪽을 고친다.
#
# 배포는 자동화하지 않는다. 서버에서 직접 한다.
name: CI

on:
  pull_request:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  ci:
    uses: Kyunglock/kwak-service-infra/.github/workflows/ci-frontend.yml@main
